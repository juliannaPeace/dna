version: '3.4'
services:
  postgres-compose:
    image: postgres:13.1
    ports:
      - "5432:5432"
    restart: always
  api-meli:
    image: juliannapaz/meli_dna:1.0
    restart: unless-stopped
    ports:
        - "8080:8080"
